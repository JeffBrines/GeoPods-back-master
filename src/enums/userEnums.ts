export enum ETypeUser {
	default = 'default',
	google = 'google',
	apple = 'apple',
	facebook = 'facebook',
}

export enum ETypeAuth {
	Registration = 'Registration',
	Login = 'Login',
	Email = 'Email',
}
